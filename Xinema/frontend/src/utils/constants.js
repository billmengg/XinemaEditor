// Video Editor Constants

// Target output resolution (width x height)
export const TARGET_WIDTH = 1080;
export const TARGET_HEIGHT = 720;
export const TARGET_ASPECT_RATIO = TARGET_WIDTH / TARGET_HEIGHT; // 1.5:1

// Aspect ratio string for CSS
export const TARGET_ASPECT_RATIO_CSS = `${TARGET_WIDTH}/${TARGET_HEIGHT}`;
